PORT=3000
FRONTEND_URL=http://localhost:5173

# PostgreSQL URL from Railway (Postgres Database -> Connect)
DATABASE_URL=postgresql://postgres:password@localhost:5432/trabajoya

# JWT Secret for local auth (generate a random string)
JWT_SECRET=super_secret_jwt_key_cambiar_en_produccion
